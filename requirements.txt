# 제조 데이터 분석 실습 앱 — 패키지 목록
# 설치: pip install -r requirements.txt
# 실행: streamlit run app.py

# 핵심 — Streamlit UI
streamlit>=1.30.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0     # .xlsx 파일 읽기

# 시각화
matplotlib>=3.7.0
plotly>=5.18.0

# 머신러닝 — 실습 ①
scikit-learn>=1.3.0

# 시계열 — 실습 ②
prophet>=1.1.5
# Prophet 의존성 (자동 설치되지만 명시)
# cmdstanpy 가 함께 깔립니다.

# 딥러닝 (선택) — LSTM 및 Hugging Face 모델용
# CPU 만 쓰는 경우 가벼운 torch 로 충분합니다.
torch>=2.0.0

# 사전학습 모델 — 실습 ③
transformers>=4.36.0
Pillow>=10.0.0      # 이미지 처리

# 객체 탐지 (YOLO) — 슬라이드 32 의 데모 코드용 (선택)
ultralytics>=8.0.0
