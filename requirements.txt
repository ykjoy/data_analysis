# 제조 데이터 분석 실습 앱 — 패키지 목록
# 설치: pip install -r requirements.txt
# 실행: streamlit run app.py

# 핵심 — Streamlit UI
streamlit>=1.30.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0           # .xlsx 파일 읽기

# 시각화
matplotlib>=3.7.0
plotly>=5.18.0

# 머신러닝 — 분류·회귀 ①, ②
scikit-learn>=1.3.0
xgboost>=2.0.0            # Gradient Boosting (RF 보다 정확도 ↑)
statsmodels>=0.14.0       # Linear Regression β·p-value 통계 분석 ★추가

# 시계열 — 실습 ③
prophet>=1.1.5
# (cmdstanpy 자동 설치)

# 딥러닝 — LSTM 및 Hugging Face 모델용 (CPU 권장)
torch>=2.0.0

# 사전학습 모델 — 실습 ④
transformers>=4.36.0
Pillow>=10.0.0            # 이미지 처리

# 객체 탐지 (YOLO) — 슬라이드 데모 코드용 (선택)
ultralytics>=8.0.0
