# 제조 데이터 분석 실습 앱 (Lite) — Streamlit Cloud
# 설치: pip install -r requirements.txt
# 실행: streamlit run app.py
#
# ※ 상한 버전(<X.Y.Z) 을 명시하면 Streamlit Cloud 의 Python 버전에 따라
#    호환 wheel 이 없을 때 pip 가 소스 컴파일로 폴백 → 빌드 무한대기.
#    하한만 명시하고 pip 가 환경에 맞는 wheel 을 받게 두는 것이 안전.

# 핵심
streamlit>=1.30.0

# 데이터
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# 시각화
matplotlib>=3.7.0
plotly>=5.18.0
koreanize-matplotlib>=0.1.1

# 머신러닝
scikit-learn>=1.3.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0

# 시계열
prophet>=1.1.5

# 딥러닝 (CPU)
torch>=2.0.0

# 사전학습 모델
transformers>=4.36.0
Pillow>=10.0.0
